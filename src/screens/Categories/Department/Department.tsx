import React, { FC } from "react";
import { View, Text, StyleSheet } from "react-native";

interface IDepartment {}
export const Department: FC<IDepartment> = ({}) => {
  return (
    <View>
      <Text>Department</Text>
    </View>
  );
};
